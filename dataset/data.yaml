# Ultralytics YOLO data descriptor for the local stratified 80/20 split.
# path is absolute so `model.val(data=..., split='val')` resolves regardless of CWD.
# Generated by benchmarks/make_split.py (seed=42, val-pct=0.2).
path: /Users/lindsaycheng/Documents/personal/projects/defect-detection/dataset/split
train: train/images
val: val/images

names:
  0: good
  1: low_water
  2: no_cap
  3: no_label